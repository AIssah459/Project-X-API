# Project X API

The backend for my Project X Web app
Written with NodeJS and Express

## Features

Requires a connection to a MongoDB database to function properly. Supports CRUD operations and JWT Authentication (HTTP) Authentication. Pulls user and event data from a database.
